fn main() {
    // 에러 발생안함.
    println!("Hello, world!")

    // 반환 값을 사용하기 때문에 세미콜론이 없는 경우 에러 발생
    // let result = println!("Hello, world!")
    // result의 타입은 `()`, 즉 반환값은 없습니다.
    // println!("Returned value: {:?}", result);
}
